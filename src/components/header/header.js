import './header.css';

const Header = () => (
    <header>
        <nav className="nav">
            <div className="links">
                {/*{navigation.map(({id, title, path}) => (*/}
                {/*    <NavLink key={id} to={path} exact={true}>{title}</NavLink>*/}
                {/*))}*/}
            </div>
        </nav>
    </header>
);

export default Header;
